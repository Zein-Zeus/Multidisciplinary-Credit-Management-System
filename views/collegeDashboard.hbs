<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>College Dashboard</title>
    <link rel="stylesheet" href="css/collegeDashboard.css" />
</head>

<body>
    <!-- NAVBAR (unchanged) -->
    <div class="nav-container">
        <nav>
            <div class="logo">
                <a href="/">
                    <img src="/images/logo.png" alt="Logo">
                </a>
            </div>
            <ul>
                <li><a href="/clgdashboard" class="active-tab">Dashboard</a></li>
                <li><a href="/clgstudentreg">New Registration</a></li>
                <li><a href="/clgstudentrecords">Student Records</a></li>
                <li><a href="/clgstudentapproval">Course Approval</a></li>
                <li><a href="/">Logout</a></li>
            </ul>
        </nav>
    </div>

    <main>
        <h1 class="greeting">Hi {{collegeName}}, here’s what’s happening today 👇</h1>

        <div class="stats-container">
            <div class="stat-card">Total Streams<br><strong>5</strong></div> <!-- If dynamic, fetch from DB -->
            <div class="stat-card">Total Courses<br><strong>{{totalCourses}}</strong></div>
            <div class="stat-card">Students Enrolled<br><strong>{{totalStudents}}</strong></div>
            <div class="stat-card">Notifications<br><strong>{{totalNotifications}}</strong></div>
        </div>

        <!-- Calendar Widget -->
        <div class="calendar-card">
            📅 <strong>Today:</strong> <span id="todayDate"></span>
        </div>

        <!-- Recent Activity -->

        <h2 class="section-heading">Recent Activity</h2>

        <!-- Pending Approvals -->
        <div class="activity-container" id="activityContainer">
            {{#if activities.length}}
            {{#each activities}}
            <div class="activity-card">{{this.icon}} {{this.message}}</div>
            {{/each}}
            {{else}}
            <p>No recent activity to show.</p>
            {{/if}}
        </div>

        <div class="notifications-panel">
            <h3>🔔 Notifications</h3>
            <ul>
                {{#if notifications.length}}
                {{#each notifications}}
                <li>{{this}}</li>
                {{/each}}
                {{else}}
                <li>No new notifications</li>
                {{/if}}
            </ul>
        </div>

        <script src="js/collegeDashboard.js"></script>
</body>

</html>